

const fetchSurveys = async (req: any, res: any) => {
    try {
        const { page, limit, searchTerm } = req.query;



        
        
    } catch (error) {
        console.error(error);
    }
}